<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ZonDiscounts — Video Upload</title>
    <style>
      :root {
        --primary-color: #ff0000;
        --secondary-color: #282828;
        --text-color: #333333;
        --background-color: #f9f9f9;
        --card-background: #ffffff;
        --shadow-color: rgba(0, 0, 0, 0.1);
      }

      body {
        font-family: "Roboto", Arial, sans-serif;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        background: var(--background-color);
        color: var(--text-color);
      }

      .container {
        text-align: center;
        background: var(--card-background);
        border-radius: 12px;
        padding: 40px;
        box-shadow: 0 8px 24px var(--shadow-color);
        max-width: 480px;
        width: 90%;
      }

      .logo {
        width: 120px;
        margin-bottom: 24px;
      }

      h1 {
        font-size: 2.5rem;
        margin-bottom: 20px;
        color: var(--secondary-color);
      }

      p {
        margin-bottom: 30px;
        font-size: 1.1rem;
        line-height: 1.6;
        color: #666;
      }

      .upload-button {
        background-color: var(--primary-color);
        color: white;
        text-decoration: none;
        padding: 14px 28px;
        font-size: 1.1rem;
        font-weight: 500;
        border: none;
        border-radius: 30px;
        cursor: pointer;
        transition: background-color 0.3s, transform 0.2s;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 12px;
      }

      .upload-button:hover {
        background-color: #e60000;
        transform: translateY(-2px);
      }

      .upload-button i {
        font-size: 1.2rem;
      }

      .spinner {
        display: none;
        width: 20px;
        height: 20px;
        border: 3px solid rgba(255, 255, 255, 0.3);
        border-radius: 50%;
        border-top: 3px solid #ffffff;
        animation: spin 1s linear infinite;
      }

      @keyframes spin {
        0% {
          transform: rotate(0deg);
        }
        100% {
          transform: rotate(360deg);
        }
      }

      footer {
        margin-top: 40px;
        font-size: 0.9rem;
        color: #999;
      }
    </style>
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container">
      <!-- Removed third-party YouTube logo to comply with branding guidelines -->
      <svg
        class="logo"
        width="120"
        height="120"
        viewBox="0 0 24 24"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        aria-hidden="true"
        focusable="false"
      >
        <rect width="24" height="24" rx="4" fill="#F3F4F6"></rect>
        <path d="M8 9.5L15 12L8 14.5V9.5Z" fill="#FF6B6B"></path>
      </svg>
      <h1>ZonDiscounts Video Uploader</h1>
      <p>
        Connect with your Google account to start uploading your videos to
        YouTube. It's fast, secure, and simple!
      </p>
      <a href="<%= url %>" class="upload-button" id="uploadButton">
        <i class="fab fa-google"></i> Continue with Google
        <div class="spinner"></div>
      </a>
      <footer>
        &copy; 2025 ZonDiscounts. All rights reserved. &nbsp;|&nbsp;
        <a href="/privacy">Privacy</a> &nbsp;|&nbsp; <a href="/terms">Terms</a>
      </footer>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/js/all.min.js"></script>
    <script>
      const uploadButton = document.getElementById("uploadButton");
      const spinner = uploadButton.querySelector(".spinner");

      uploadButton.addEventListener("click", (e) => {
        e.preventDefault();
        spinner.style.display = "inline-block";
        uploadButton.style.pointerEvents = "none";
        setTimeout(() => {
          window.location.href = uploadButton.href;
        }, 1000);
      });
    </script>
  </body>
</html>
